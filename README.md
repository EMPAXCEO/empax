# Empax — Test Server

Run a minimal static test server for local development.

Start the server:

```powershell
npm install   # optional, no deps required
npm start
```

Open http://localhost:3000/ in your browser. Set `PORT` env var to change the port.
